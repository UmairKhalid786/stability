plugins {
    `kotlin-dsl`
}

repositories {
    gradlePluginPortal()
    google()
    mavenCentral()
}

dependencies {
    // Add what your task needs to compile against.
    // Examples (ONLY if your task references Android/AGP or Compose tooling APIs):
    // implementation("com.android.tools.build:gradle:8.5.2")
}